---
"flint": minor
---

Playground now exposes generation parameters — temperature, max tokens, top-p, top-k, frequency penalty, presence penalty, and a fixed sampling seed — in a collapsible panel next to the Context control, persisted per conversation the same way the model and system prompt are.
