---
"flint": minor
---

Playground now exposes generation parameters — temperature, max tokens, top-p, and top-k — in a collapsible panel next to the Context control, persisted per conversation the same way the model and system prompt are.
