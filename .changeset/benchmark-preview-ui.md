---
"flint": minor
---

Add a Benchmark Preview UI (opt-in, off by default) under Build, alongside Playground and Model Arena, for measured, repeatable multi-model benchmark runs with a hardened Stop/Resume lifecycle.
