---
"flint": patch
---

A local endpoint request for a cached build can load that build when another build of the same model is idle. Diagnostics → Test local endpoint runs the chat, embedding, and speech checks against every model id and parent alias the endpoint lists.
