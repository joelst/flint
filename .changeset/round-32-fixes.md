---
"flint": patch
---

Prevent a benchmark run from being permanently stuck when finishing it takes too long, and fix a case where cleanup could re-open external API access to a model pool mid-benchmark.
