---
---

Document that 0.7.0 ships as a GitHub prerelease outside the in-app updater channel and must be installed manually, and pass a file:// URL to the sidecar test loader so the suite passes on Node 23+.
