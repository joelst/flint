---
"flint": patch
---

Add a Settings → Startup toggle ("Check model catalog automatically") to disable the automatic startup check against Microsoft's Foundry Local model catalog. Off by choice, the manual "Refresh catalog" button in the Models view remains available. Docs now disclose this network call as the one built-in exception to Flint's local-only inference.
