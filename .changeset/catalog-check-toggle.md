---
"flint": patch
---

Add a Settings → Startup toggle ("Check model catalog on startup") to disable the automatic startup check against Microsoft's Foundry Local model catalog. Recommendations and configured model preloads are skipped for that launch when disabled. Docs now disclose when catalog network access occurs.
