---
"flint": patch
---

Update the bundled Foundry Local SDK from 1.2.4 to 2.0.1 so models such as Gemma 4 can load. Long native load errors stay in the app log; the header shows one truncated line instead of growing to fit the stack trace.
