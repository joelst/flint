---
"flint": patch
---

Recognize newer Nemotron ASR model names (e.g. `nemotron-3.5-asr-streaming-0.6b`) as speech models instead of chat, matching an `-asr-` marker in addition to the original `-speech-` naming.
